[build]
  command = "npm run build"
  publish = "frontend/build"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"

[dev]
  targetPort = 3000
  port = 8888

# Single page app redirect - MUST BE LAST
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200